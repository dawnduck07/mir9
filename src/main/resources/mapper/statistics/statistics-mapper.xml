<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="statistics">
  <select id="selectPeriodStatistics" resultType="periodStatisticVo">
  	SELECT *, SUM(amount) AS total_amount, SUM(cancel_amount) AS total_cancel_amount FROM v_period_statistics  WHERE date(paid_at) = date(#{date}) GROUP BY paid_at
  </select>
</mapper>